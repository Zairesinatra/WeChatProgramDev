<button type="primary" size="default" style="width:100vw" bindtap="changeCountval">修改message页面中count的值:{{count}}</button>
<view>===================================</view>

<custom-test bind:myevent="getSonCount" prop-count="{{count}}" class="customA" id="cA">
  <view>这是通过插槽渲染的view标签</view>
  <view slot="before">插入before插槽</view>
  <view slot="after">插入after插槽</view>
</custom-test>
